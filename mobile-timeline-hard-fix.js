/* Retired: the old hard-fix used a fixed 106px gutter and repeatedly rewrote inline styles.
   That caused itinerary restaurant/detail cards to shift too far right and clip their content.
   Geometry is now handled by mobile-itinerary-final-geometry.js. */
(function(){
  'use strict';
  // Intentionally no layout mutation here. Keep this file for compatibility with index.html.
})();
